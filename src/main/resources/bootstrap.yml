spring:
  application:
    name: markdown-editor
  cloud:
    config:
      enabled: true
server:

  servlet:
    context-path: /${spring.application.name}

management:
  endpoints:
    web:
      base-path: /actuator
